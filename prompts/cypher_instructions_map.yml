parameters:
  - cypher_instructions # prompts/cypher_instructions.yml
prompt: |+
  <instructions>
  {cypher_instructions}
  Always return latitude and longitude for the sites since they represent the x and y coordinates for the map plot.
  Always return site names for the sites since they will be used as information provided on hovering events over the
  points on the map.
  </instructions>
